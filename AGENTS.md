# AI Agent Instructions for anoju-react

이 문서는 **anoju-react** 프로젝트에서 코드를 작성하고 수정할 때 반드시 준수해야 하는 지침을 담고 있습니다. 모든 AI 에이전트는 작업 시 아래 규칙을 철저히 따라야 합니다.

## 1. 프로젝트 개요 (Project Summary)

- **목적:** 모바일 최우선(Mobile-First) 반응형 웹 애플리케이션 개발.
- **기술 스택:** React, Vite, TypeScript, SCSS (Vanilla SCSS 사용), **ahooks**, **Framer Motion**, **Embla Carousel**, **Tiptap**.
- **패키지 매니저:** `pnpm`을 사용하며, `pnpm-lock.yaml`을 단일 잠금 파일로 관리합니다.
- **주요 기능:** SNS/이메일 회원가입(인증 포함), 자유게시판/갤러리(에디터 적용), 정적 콘텐츠 페이지 관리.
- **운영 배포:** 프론트엔드는 시놀로지 NAS에 배포하며 운영 도메인은 `https://anoju.synology.me`를 사용합니다.

## 1.1 작업 요청 구분 원칙 (Design vs Implementation)

- **설계 요청:** 사용자가 `설계`의 수정 또는 추가를 요청하는 경우, 구현 코드는 작성하지 않고 `plan.md`와 `AGENTS.md`만 업데이트합니다.
- **구현 금지 조건:** 사용자가 명시적으로 `구현`을 요청하기 전까지는 컴포넌트, 훅, 스타일, 라우팅, 데이터 연동 등 실제 구현 작업을 진행하지 않습니다.
- **구현 요청:** 사용자가 `구현`을 명시적으로 요청하면, 해당 요청의 to-do 리스트를 먼저 작성하고 순서대로 진행합니다.
- **의사결정 확인:** 구현 방식, 화면 동작, 공통 구조처럼 선택지가 있는 결정은 사용자의 확인을 받은 뒤 확정합니다.

## 2. 디자인 및 테마 컨셉 (Design & Theme)

- **디자인 방향**: **Warm Minimalism**의 감성과 **Modern Essential**의 편의성을 결합하며, **퍼플(Purple)**을 시그니처 포인트 컬러로 사용합니다.
- **UI 컴포넌트 구현 원칙**: UI 컴포넌트 라이브러리는 직접 도입하지 않고 프로젝트 내부에서 직접 구현합니다.
- **UI 참조 기준**: 컴포넌트 API, 이벤트 명명, 상태 속성, 접근성 동작은 **Radix UI**를 1차 기준으로 참고합니다. 접근성이 복잡한 컴포넌트는 **React Aria**를 보조 기준으로 검증하고, 모바일 UX 흐름은 **Ant Design Mobile**을 참고합니다. **Shadcn UI**는 Radix 기반 조합 방식만 참고하며 디자인과 코드는 복제하지 않습니다.
- **공통 UI 1차 범위**: 우선 구현 대상 공통 UI는 `Button`, `IconButton`, `Img`, `Input`, `TextArea`, `Checkbox`, `Radio`, `Switch`, `Tabs`, `Select`, `Toast`, `Dialog`, `Alert`, `Confirm`, `BottomSheet`, `Spinner`, `GlobalLoading`, `PageLoading`, `EmptyState`, `ErrorState`, `DataList`, `AppHeader`, `FloatingMenu`, `Page`, `VisuallyHidden`입니다.
- **공통 UI API**: 공통 컴포넌트는 `size="xs|sm|md|lg|xl"`, `variant="solid|soft|outline|ghost|plain"`, `tone="primary|neutral|danger|success|warning|info"`를 기본 축으로 삼고, 상태는 `data-state`, `data-disabled`, `data-loading`, `data-selected`, `data-active`, `data-invalid`로 표현합니다.
- **공통 피드백 정책**: 저장/복사 등 짧은 결과는 `Toast`, 단순 안내는 `Alert`, 삭제/로그아웃/작성 취소처럼 결정이 필요한 경우는 `Confirm`, 모바일 액션 선택은 `BottomSheet`, 페이지/섹션 실패는 `ErrorState`, 데이터 없음은 `EmptyState`, 짧은 진행 상태는 `Spinner`를 사용합니다.
- **Dialog 사용 원칙**: `Dialog`는 콘텐츠나 복합 UI를 담는 범용 모달 컨테이너로 사용합니다. 안내 메시지와 확인/취소 메시지는 원시 `Dialog`를 직접 쓰지 않고, 사용성이 편한 `Alert`와 `Confirm` 래퍼를 사용합니다.
- **로딩 정책**: 로고 기반 인터랙션을 가진 `GlobalLoading`을 전역 로딩의 시각 컴포넌트로 사용하고, 화면에서는 `PageLoading` 래퍼를 통해 호출합니다. 전체 페이지를 막는 `PageLoading`은 인증 초기화, 보호 라우트 판정, 필수 초기 데이터 확인, 상세/수정 화면의 첫 데이터 조회처럼 화면 렌더링 자체가 불가능한 경우에만 사용합니다. 버튼 액션은 `Button loading`, 리스트 추가 로딩은 리스트 하단 `Spinner` 로딩을 사용합니다. 이미 데이터가 보이는 상태의 재조회, 무한스크롤 추가 조회, 비디오 내부 버퍼링, 작은 위젯 로딩에는 `GlobalLoading/PageLoading`을 사용하지 않습니다.
- **Skeleton 정책**: 짧은 Skeleton 노출은 오류처럼 보일 수 있으므로 기본 1차 컴포넌트에서는 제외하고, `500ms` 이상 예상되는 리스트/상세 구조에서만 2차로 제한 검토합니다.
- **DataList 정책**: 리스트형 화면은 공통 `DataList`를 사용하며, 1차로 `mode="infinite"`와 `mode="loadMore"`를 지원합니다. 첫 조회 실패는 `ErrorState`, 데이터 없음은 `EmptyState`, 추가 조회 실패는 기존 목록 유지 후 하단 재시도 액션으로 처리합니다.
- **폼 검증 정책**: 입력 중 과한 에러 노출을 피하고, `blur` 시 필드 검증, `submit` 시 전체 검증을 수행합니다. 제출 실패 시 첫 번째 에러 필드로 포커스를 이동하고, 필드 에러와 폼 에러를 분리합니다.
- **아이콘 정책**: 아이콘은 `lucide-react`를 기본 라이브러리로 사용하며, 아이콘 단독 버튼은 `IconButton`과 접근성 이름을 반드시 사용합니다. 특수 브랜드/서비스 아이콘을 제외하고 직접 SVG 작성을 지양합니다.
- **테마 지원**: 라이트(Light), 다크(Dark), 시스템 설정(Auto) 모드를 완벽 지원하며, `html`의 `data-theme="light|dark|auto"` 속성과 CSS 변수(`var(--color-...)`)를 필수적으로 활용합니다.
- **다크모드 컬러 제한**: 다크모드는 차콜, 쿨 그레이, 퍼플 기반으로 구성하며 브라운/에스프레소/탄/오렌지 브라운 계열을 배경, 텍스트, 테두리의 기본 컬러로 사용하지 않습니다.
- **글자모드 지원**: 글자모드는 `더작게`, `작게`, `기본`, `크게`, `더크게` 5단계를 지원하며, `html`의 `data-font-mode="xsmall|small|base|large|xlarge"` 속성과 CSS 변수(`var(--font-size-*)`, `var(--line-height-*)`)로 제어합니다. `--font-size-md` 기준 크기는 각각 `12px`, `14px`, `16px`, `18px`, `20px`입니다.
- **CSS 변수 단일 관리**: 디자인 토큰 CSS 변수는 `src/assets/styles/base/_tokens.scss`에서만 정의/수정하고, 다른 SCSS 파일에서는 토큰을 참조만 합니다.
- **토큰 네이밍 기준**: 색상 토큰은 `--color-text-*`, `--color-bg-*`, `--color-border-*`, `--color-icon-*`, `--color-action-*`처럼 역할별로 세분화하고, 글자 크기는 `--font-size-caption`, `--font-size-body-md`, `--font-size-title-lg`, `--font-size-heading-md`, `--font-size-display-sm`처럼 사용처 기반으로 관리합니다.
- **글자모드 레이아웃 대응**: 글자모드 변경 시 텍스트 높이, 줄 수, sticky 위치, 고정 영역 오프셋, 플로팅 버튼 위치가 달라질 수 있으므로 관련 컴포넌트는 크기와 위치를 다시 계산할 수 있게 설계합니다.
- **이미지 컴포넌트 필수 사용**: 모든 이미지는 공통 `Img` 컴포넌트를 통해 렌더링하며, 라이트/다크 모드별 이미지 소스가 필요한 경우 `Img` 컴포넌트에서 테마별 소스와 `alt`를 일관되게 처리합니다.
- **시각적 언어**: 부드러운 곡선과 적절한 여백, 현대적인 글래스모피즘 효과를 권장합니다.
- **마이크로 인터랙션**: **Framer Motion**을 사용하여 부드럽고 예측 가능한 인터랙션(탭 피드백, 페이지 트랜지션 등)을 구현합니다.
- **숨김/노출 인터랙션:** 사용자가 `숨김`, `노출`, `숨겼다 보여졌다`, `나타났다 사라졌다`처럼 표현한 UI 동작은 단순 표시 전환이 아니라 Framer Motion 또는 CSS Transition 기반의 자연스러운 인터랙션이 적용된 형태로 구현합니다.
- **스와이프 UI:** 이미지 피드/상세처럼 좌우 스와이프가 필요한 UI는 직접 드래그 로직을 구현하지 않고 **Embla Carousel**을 사용합니다. 스타일과 버튼/도트 UI는 프로젝트 컴포넌트에서 직접 관리합니다.

## 2.1 전역 화면 레이아웃 및 고정 영역 정책 (Global Layout & Fixed Areas)

- **설계 우선 원칙:** 전역 레이아웃, 헤더, 하단 플로팅 버튼, 고정 영역 관리 로직은 구현 전 반드시 `plan.md`에 먼저 설계 내용을 업데이트하고, 주요 의사결정이 필요한 경우 사용자 확인을 받은 뒤 진행합니다.
- **상단 헤더:** 헤더는 기본적으로 상단 고정 영역으로 동작하되, 아래로 스크롤할 때 숨기고 위로 스크롤할 때 다시 나타나는 동작을 지원해야 합니다.
- **헤더 고정 옵션:** 특정 화면에서는 스크롤 방향과 무관하게 헤더를 항상 고정 노출할 수 있는 옵션을 제공해야 합니다.
- **Sticky 콘텐츠 연동:** 본문 내부의 `sticky` 성격 콘텐츠와 헤더의 숨김/노출 상태가 충돌하지 않도록 상단 고정 영역의 높이, 오프셋, z-index를 중앙에서 관리합니다.
- **헤더 구성:** 좌측 영역은 `뒤로가기 버튼`, `페이지 타이틀`, `좌측 확장 영역` 순서로 배치하고, 우측 영역은 `우측 확장 영역`, `알림 버튼`, `홈 버튼` 순서로 배치합니다.
- **헤더 요소 제어:** 뒤로가기 버튼, 페이지 타이틀, 홈 버튼, 좌우 확장 영역은 화면별 요구에 따라 숨김 처리하거나 커스텀 콘텐츠를 주입할 수 있어야 합니다.
- **헤더 알림 버튼:** 홈 화면의 메인 헤더 우측에는 알림함으로 이동하는 `IconButton`을 제공합니다. 비로그인 사용자가 누르면 보호 라우트를 통해 로그인 화면으로 이동하고, 로그인 사용자는 알림함으로 이동합니다. 읽지 않은 알림이 있으면 접근성 이름과 시각 배지를 함께 갱신합니다. 알림 버튼은 기본적으로 홈 화면에서만 노출하고, 노출 여부는 라우트/레이아웃 설정에서 화면별로 제어합니다.
- **뒤로가기 동작:** 뒤로가기는 기본적으로 `history.back()`을 사용하되, 화면별 설정에 따라 특정 경로 이동 또는 커스텀 핸들러로 대체할 수 있어야 합니다.
- **전체메뉴 버튼 제외:** 전체메뉴는 별도 페이지로 관리하므로 전역 헤더에는 전체메뉴 버튼을 두지 않습니다.
- **모바일 푸터 정책:** 모바일 UI에는 기본 푸터를 제공하지 않습니다.
- **하단 플로팅 버튼:** 하단 플로팅 버튼은 모든 화면에 배치하지 않고, `홈` 화면과 플로팅 메뉴 항목으로 직접 이동하는 주요 화면에서 기본 노출합니다. 기본 메뉴는 `전체메뉴`, `lounge`, `Snaps`, `마이페이지`로 구성하며, 기존 `홈` 항목은 제외합니다. 자유게시판 목록/상세/작성/수정, Pics 피드/상세/작성, picLog 목록/상세/작성/수정 등 콘텐츠 소비/작성 중심의 하위 화면에서는 기본 미노출하되, 라우트/레이아웃 설정에서 화면별로 재정의할 수 있어야 합니다.
- **플로팅 메뉴 콘텐츠 진입:** `lounge`는 커뮤니티형 게시판 묶음의 1뎁스 서브 메인으로 이동합니다. `Snaps`는 시각 콘텐츠 1뎁스 서브 메인으로 이동합니다. 플로팅 메뉴에서 `자유게시판`, `Pics`, `picLog` 같은 2뎁스 화면으로 바로 이동하지 않습니다.
- **로그인 전 메뉴:** 하단 플로팅 버튼의 `마이페이지` 항목은 로그인 전에는 `로그인` 메뉴로 노출하고, 로그인 후에는 `마이페이지` 메뉴로 노출합니다.
- **보호 메뉴 인증 체크:** `마이페이지`처럼 로그인이 필요한 메뉴와 화면은 진입 전에 반드시 인증 상태를 확인하고, 미로그인 상태라면 로그인 화면으로 이동시켜야 합니다.
- **플로팅 버튼 스크롤 동작:** 하단 플로팅 버튼도 헤더와 동일하게 아래로 스크롤할 때 숨기고 위로 스크롤할 때 나타나는 동작을 지원해야 합니다.
- **하단 고정 영역 관리:** 플로팅 버튼 외에도 콘텐츠 내부 하단 고정 영역이 존재할 수 있으므로, 상단 고정 영역 관리와 함께 하단 고정 영역의 높이, 안전 영역, 간격, z-index를 중앙에서 관리하여 겹침을 방지합니다.
- **하단 액션 우선순위:** 주요 메인 액션은 하단 고정 버튼 영역으로 제공합니다. 하단 요소가 동시에 존재할 경우 화면 아래 기준으로 `플로팅 메뉴바`, `픽스트 버튼`, `플로팅 btns` 순서로 위로 쌓습니다. 토스트가 하단에 뜰 때는 하단 고정 요소 위로 노출하며, 복잡한 화면에서는 플로팅 btns 위로 띄웁니다.
- **플로팅 btns:** 글쓰기처럼 화면의 보조 주요 액션은 우측 하단 원형 `+` 플로팅 버튼으로 제공할 수 있으며, 플로팅 메뉴바와 동시에 노출되는 화면에서는 플로팅 메뉴바 위쪽 오프셋을 기준으로 배치합니다.
- **하단 액션 공통화:** 플로팅 btns와 하단 고정 액션 영역은 화면별 직접 마크업/스타일로 만들지 않고 공통 `FloatingActions`, `FloatingActionButton`, `FixedBottomActions` 컴포넌트를 사용해 관리합니다.
- **하단 고정 버튼 크기:** `FixedBottomActions` 안에 들어가는 버튼은 반드시 큰 사이즈(`lg`)로 사용하며, 화면별로 작은 버튼을 지정하지 않습니다.
- **게시판 메뉴 구조:** 커뮤니티형 게시판 묶음은 1뎁스 `lounge`, 하위 2뎁스 첫 메뉴는 `자유게시판`, 추가 2뎁스 메뉴는 `디바이스정보`로 구성합니다. 이미지/소셜 피드 묶음은 1뎁스 `Snaps`, 하위 2뎁스 첫 메뉴는 `Pics`, 추가 2뎁스 메뉴는 `picLog`로 구성합니다. `lounge`와 `Snaps`는 추후 2뎁스 메뉴 확장을 전제로 각각 서브 메인 화면을 가져야 합니다.
- **콘텐츠 시간 표기:** 자유게시판, Pics, picLog, 댓글, 마이페이지 활동 목록의 사용자 노출 시간은 기본적으로 `5분 전`, `2시간 전` 같은 상대 시간으로 통일합니다. 오래된 콘텐츠처럼 절대 날짜가 필요한 경우에만 보조 정보 또는 title 속성으로 절대 시간을 제공합니다.
- **내 게시물 보기:** 자유게시판, Pics, picLog를 포함한 모든 게시판/갤러리 목록 화면은 `내 게시물 보기` 기능을 제공해야 합니다. 이 기능은 현재 로그인한 사용자가 작성한 게시물만 필터링하며, 검색/정렬/태그와 동일하게 URL query와 동기화하고 변경 시 `DataList`의 페이지/커서 상태를 초기화합니다. 미로그인 사용자가 진입하면 로그인 화면으로 이동시키고 로그인 후 원래 목록으로 복귀할 수 있어야 합니다. 마이페이지의 `내가 작성한 글`은 전체 작성글 모아보기이고, 각 게시판/갤러리의 `내 게시물 보기`는 해당 목록 안에서 동작하는 필터로 구분합니다.
- **알림함 UX:** `/my-page/notifications`는 로그인한 사용자만 접근할 수 있는 알림함 화면으로 사용합니다. 내 글에 댓글/대댓글이 달린 경우, 내 댓글에 대댓글이 달린 경우, 누군가 본문/댓글/picLog 메모에서 나를 태그한 경우, picLog 초대 또는 참여자 순서 변경 요청이 온 경우를 1차 알림 대상으로 둡니다. 알림은 최신순 목록, 읽음/읽지 않음 상태, 상대 시간, 연결 경로를 제공하고, 자기 자신이 만든 액션은 기본 알림에서 제외합니다. 전체메뉴에는 `Members` 섹션 아래에 `알림함` 메뉴를 추가하고, 마이페이지 서브 메인에도 `알림함` 이동 메뉴를 제공합니다.
- **자유게시판 UX:** `자유게시판`은 일반 커뮤니티 게시판처럼 목록, 상세, 작성, 수정, 삭제, 댓글을 제공하며, 리치 텍스트 에디터를 통해 글 중간 이미지 삽입을 지원합니다. 이미지 외 일반 파일은 별도 파일첨부 목록으로 관리합니다. 목록은 모바일 중심의 단순 리스트 형태로 만들고 테이블형 레이아웃은 사용하지 않습니다. 작성/댓글 작성은 로그인 및 이메일 인증 완료 회원만 가능하고 조회는 모든 사용자가 가능합니다. 수정은 작성자 본인과 관리자만 가능하며 `/lounge/free-board/:postId/edit` 경로에서 제목과 본문을 수정합니다. 목록과 상세의 회원 닉네임 좌측에는 프로필 이미지를 원형 아바타로 함께 노출합니다. 상세 본문 하단에는 `좋아요`, `싫어요`, `공유하기` 액션을 함께 배치합니다.
- **디바이스정보 UX:** `디바이스정보`는 일반 게시판이 아니라 모바일 디바이스 웹 해상도 데이터 목록/상세/작성 중심으로 제공합니다. 작성은 로그인 및 이메일 인증 완료 회원만 가능하고 조회는 모든 사용자가 가능합니다. 수정은 작성자 본인과 관리자만 가능하며 `/lounge/device/:deviceReportId/edit` 경로에서 제조사, 모델, 스크린 사이즈, 윈도우 사이즈, ratio, 스크린방향, userAgent, 디스플레이설정, 추가설명을 수정합니다. 제조사와 모델은 이전 저장값 선택 + 직접입력 방식을 지원하고, 스크린 사이즈/윈도우 사이즈/ratio/스크린방향/userAgent는 자동 입력합니다. 수정 화면에서는 자동 등록값도 직접 입력할 수 있으며, 윈도우 사이즈는 현재 브라우저 기준으로 다시 측정할 수 있어야 합니다. 윈도우 사이즈는 작성 중 resize와 orientationchange 때 관측값을 갱신하여 최소/최대값을 저장합니다. 디스플레이설정은 Android 등 설정의 화면 크기/표시 크기 단계 기준이며, 설정에서 가장 왼쪽을 `1`로 보고 현재 단계를 입력합니다. iPhone나 iPad등 설정을 바꿀수 없을경우 `0`으로 입력합니다. 목록은 제조사, 모델, 윈도우 사이즈, ratio, 스크린방향, 디스플레이설정을 보여주고 제조사/모델 필터와 검색 기능을 URL query와 동기화합니다. 상세의 일반 조회 액션은 공유하기만 제공하고, 작성자 본인과 관리자에게만 수정/삭제 액션을 추가로 노출합니다.
- **닉네임 및 사용자 태그 정책:** 사용자 노출명과 댓글 태그 식별자는 `nickname`만 사용합니다. 별도 `name` 사용자 필드는 사용하지 않고 제거합니다. 닉네임은 회원가입 및 마이페이지 수정 시 필수 입력으로 받고, 한글/영문 소문자/숫자/밑줄 조합 2~20자로 제한하며, 저장 전 중복 확인을 수행합니다. 태그 표기는 `@nickname` 형식을 사용합니다. PocketBase `users.nickname` 필드는 서버에서도 unique 제약을 유지해야 합니다.
- **댓글 UX:** 댓글 항목에는 작성자 닉네임 좌측 프로필 이미지, 내용, 상대 시간과 함께 `좋아요`, `싫어요` 액션을 제공합니다. 게시글 작성자의 댓글은 `글쓴이` 배지와 하이라이트로 구분합니다. 댓글은 대댓글 작성을 지원하며 `parentComment` 관계로 묶어 들여쓰기 형태로 표시합니다. 댓글 반응은 로그인 및 이메일 인증 완료 회원만 사용할 수 있고, 같은 대상에 대한 좋아요/싫어요는 상호 배타적으로 처리합니다. 댓글 수정과 삭제는 작성자 본인 및 관리자만 사용할 수 있으며, 삭제 전에는 반드시 `Confirm`을 사용하고 soft delete로 숨김 처리합니다.
- **Pics UX:** `Pics`는 인스타그램 같은 소셜 피드 UI를 기준으로 작성자 닉네임 좌측 프로필 이미지, 이미지, 캡션, 좋아요, 싫어요, 댓글, 스크랩, 공유 액션을 제공합니다. 작성 화면에서는 사용자 입력 제목을 받지 않고 이미지 업로드/대표 이미지 지정 후 가장 아래에 캡션 작성 영역을 둡니다. 수정은 작성자 본인과 관리자만 가능하며 `/snaps/pics/:postId/edit` 경로에서 캡션을 수정하고 기존 이미지는 확인용으로 보여줍니다. 이미지 필터/보정/편집 기능은 제공하지 않고 업로드, 순서 관리, 대표 이미지, 확대 보기 중심으로 제한합니다. 상세 본문 하단에는 `좋아요`, `싫어요`, `공유하기` 액션을 함께 배치합니다.
- **picLog UX:** `picLog`는 setlog의 짧은 순간 기록 감성을 사진 중심으로 재해석한 하루 사진 로그입니다. Pics가 공개 이미지 피드라면 picLog는 날짜, 친구/그룹, 시간 챕터를 중심으로 여러 사용자가 같은 하루를 사진으로 채우는 공동 기록 기능입니다. 기본 경로는 `/snaps/pic-log`로 두고 생성은 `/snaps/pic-log/new`, 상세는 `/snaps/pic-log/:logId`, 사진 추가는 `/snaps/pic-log/:logId/add`, 수정은 `/snaps/pic-log/:logId/edit`, 초대 입장은 `/snaps/pic-log/:logId/join`을 사용합니다. 시간 챕터는 `08:00`부터 `22:00`까지 1시간 단위로 제공하고, 사진이 없는 시간과 날짜는 UI에 노출하지 않습니다. 시간 챕터 이동은 좌우 스와이프를 기본으로 하며 Embla Carousel을 사용합니다. 날짜 이동은 등록된 사진이 있는 날짜만 보여주고, 기본 조회 기간은 최근 14일을 권장합니다. 사진 추가는 순간 단위 1장 업로드와 짧은 메모 입력을 기본 흐름으로 하며, 등록한 사진과 메모는 작성자 본인과 관리자만 수정할 수 있습니다. 참여자 표시 순서는 입장 시 정렬 기준으로 사용하고, 순서 변경은 바꾸려는 위치의 사용자에게 요청을 보내 상대가 수락했을 때만 교환합니다. 공개 범위는 `나만 보기`, `초대된 친구만`, `링크를 아는 사람`, `전체 공개` 후보를 두되 기본값은 `초대된 친구만`입니다. 초대형 로그는 방 단위 초대 패스워드를 가지며, 상세 상단 초대 아이콘을 누르면 `Dialog`로 초대 링크와 패스워드를 함께 보여주고 복사 액션을 제공합니다. 초대 링크로 들어온 사용자는 이미 참여자인 경우 상세로 바로 이동하고, 아직 참여자가 아니면 패스워드 입력 화면에서 확인 후 참여자로 추가됩니다. 초대 패스워드 검증은 PocketBase update rule에서 처리합니다. 댓글은 시간 챕터 단위로 제공하며 `@nickname` 사용자 태그를 지원합니다. 사진 영역의 댓글 아이콘 버튼을 누르면 하단 댓글 입력으로 스크롤 이동하고 해당 사진 작성자의 닉네임 태그를 자동 삽입합니다. 로그 생성, 사진 추가, 반응, 댓글 작성은 로그인 및 이메일 인증 완료 회원만 가능하고, 조회 권한은 공개 범위를 따릅니다. 삭제는 soft delete를 기본으로 하며 로그 삭제와 사진 삭제 모두 `Confirm`을 사용합니다.
- **picLog 5초 영상 촬영 후보:** picLog에는 추후 사진 1장 흐름을 유지하되, 선택 기능으로 `5초 순간 영상`을 붙일 수 있게 설계합니다. 실제 화면 연결 전에는 독립 기능으로 `useTimedVideoRecorder` 훅과 `TimedVideoCapture` 컴포넌트를 먼저 만들고, `/snaps/pic-log/:logId/add`에는 사용자가 명시적으로 구현을 요청한 뒤 연결합니다. 촬영은 HTTPS 환경에서 `navigator.mediaDevices.getUserMedia()`와 `MediaRecorder`를 사용하며, 기본 카메라는 `facingMode: "environment"`, 최대 녹화 시간은 5초, 완료 시 `Blob`과 미리보기 URL을 반환합니다. iOS Safari 등 `MediaRecorder` 지원이 불완전한 환경에서는 `input type="file" accept="video/*" capture="environment"` 대체 흐름을 제공하되, 이 방식은 5초 자동 종료가 불가능함을 UI에서 안내합니다. 구현 파일에는 한글 주석으로 권한 요청, 녹화 시작/자동 종료, 스트림 track 정리, object URL 해제, 미지원 브라우저 대체 흐름을 설명해야 합니다.
- **마이페이지 구조:** `/my-page`는 마이페이지 1뎁스 서브 메인으로 사용하고, `/my-page/profile`, `/my-page/posts`, `/my-page/comments`, `/my-page/notifications`처럼 기능별 화면을 분리합니다. 회원정보 수정 화면에서는 프로필 이미지를 등록/수정/삭제할 수 있어야 하며, 업로드 시 정사각형 크롭 UI를 제공합니다. `내가 작성한 글`에는 자유게시판, Pics, picLog 작성글을 함께 노출합니다. 플로팅 메뉴의 `마이페이지`는 서브 메인으로 이동하며, 기능별 하위 화면은 기본적으로 플로팅 메뉴를 노출하지 않습니다.
- **관리자 화면 구조:** 관리자 전용 화면은 `/admin` 하위에 두고 `requiresAuth: true`, `adminOnly: true`, `robots: "noindex"`로 설정합니다. 1차 하위 화면은 `/admin/content-settings`, `/admin/reports`, `/admin/members`, `/admin/members/:userId`를 기준으로 설계합니다.
- **콘텐츠 운영 설정:** `자유게시판`, `디바이스정보`, `DevLog`, `Pics`, `picLog`, `Clips`를 포함한 현재의 모든 게시판/갤러리와 추후 추가되는 모든 게시판/갤러리는 관리자 설정 대상으로 등록해야 합니다. 관리자는 작성/수정/삭제 권한, 보기 권한, 댓글 노출 및 작성 허용, 좋아요/싫어요 노출 및 사용 허용, 공유하기 노출, 신고하기 노출, 목록 노출, 운영 상태를 설정할 수 있어야 합니다. 프론트 노출 제어만으로 권한을 처리하지 않고, 작성/수정/삭제/보기 권한은 PocketBase API Rules 또는 서버 hook에서도 반드시 검증합니다.
- **신고 규칙:** 신고는 로그인 및 이메일 인증 완료 사용자만 사용할 수 있고, 본인이 작성한 대상은 신고할 수 없으며, 같은 사용자는 같은 대상에 중복 신고할 수 없습니다. 신고 대상은 게시글, 댓글, Pics, picLog, Clips, Clips 댓글, 회원을 포함하고, 신고 사유는 고정 목록과 상세 입력을 함께 사용합니다. 신고 접수만으로 자동 삭제하지 않고 관리자 검토 후 숨김/삭제/회원 경고/회원 정지/기각/메모 기록 등의 조치를 수행합니다.
- **회원 밴 정책:** 회원 차단은 `users.status = "suspended"`를 기본으로 사용합니다. 관리자 화면에서는 회원 목록, 검색, 상태 필터, 정지/해제, 정지 사유, 정지 기간, 관리자 메모, 작성 콘텐츠 확인을 제공해야 하며, 정지 사용자는 글쓰기/댓글/반응/신고 등 쓰기 액션을 사용할 수 없습니다.

## 3. 언어 정책 (Language Policy)

- **기본 언어:** 사용자 환경 및 코드 내 모든 설명, 주석, README는 반드시 **한글(Korean)**을 사용합니다.
- **표현 방식:** 간결하고 명확한 한글 문장으로 지침과 정보를 전달합니다.

## 4. 파일 및 폴더 체계화 (File & Folder Organization)

모든 개발 작업 시 파일의 증가에 대비하여 체계적으로 폴더를 구성하고 분류합니다.

- **이미지 및 스타일 에셋:** `src/assets/images`, `src/assets/styles` 폴더를 기본으로 하며, 스타일 하위에 `base`(기본), `layout`(레이아웃), `components`(컴포넌트), `pages`(페이지별) 폴더를 생성하여 관리합니다.
- **SCSS 단일 관리:** 모든 SCSS 파일은 반드시 `src/assets/styles` 아래에서만 작성하고 관리합니다. 컴포넌트 폴더, 페이지 폴더, 기타 기능 폴더 내부에는 SCSS 파일을 생성하지 않습니다.
- **공통 요소 폴더:** 여러 화면과 기능에서 공유되는 요소는 `src/apis`, `src/data`, `src/types`, `src/constants`, `src/utils`, `src/hooks`, `src/lib` 아래에서 역할별로 관리합니다.
- **라우팅 설정:** 라우트 경로, 로그인 필요 여부, 헤더/플로팅 메뉴 기본 설정은 `src/routes/routeConfig.ts`에서 TypeScript 설정으로 중앙 관리합니다.
- **페이지 메타:** `document.title`, description, robots 정책은 `routeConfig.ts`의 `meta` 객체에서 관리합니다. 모든 페이지는 `meta.title`을 가지며 최종 타이틀은 `페이지명 | Anoju` 형식을 사용합니다.
- **인증 가드:** `requiresAuth: true`인 라우트는 진입 전에 인증 상태를 확인하고, 미로그인 상태라면 로그인 화면으로 이동시키며 로그인 후 복귀할 경로를 보존합니다.
- **화면 폴더:** 화면 컴포넌트는 `src/pages/화면명/Index.tsx` 구조를 사용하며, 화면명 폴더는 PascalCase를 사용합니다. 예: `src/pages/Home/Index.tsx`
- **메뉴별 화면 그룹:** 화면이 늘어나는 메뉴/도메인은 `src/pages/메뉴명/화면명/Index.tsx`처럼 상위 폴더로 묶어 관리합니다. 예: `src/pages/Lounge/FreeBoard/Index.tsx`, `src/pages/Snaps/Pics/Write/Index.tsx`, `src/pages/MyPage/Profile/Index.tsx`
- **독립 화면 그룹:** 로그인/회원가입은 `src/pages/Auth`, 시스템성 오류 화면은 `src/pages/System`, 정적 콘텐츠는 `src/pages/Static` 아래로 분류합니다.
- **라우트 경로:** 실제 화면 경로는 폴더명과 분리하여 소문자 kebab-case를 사용합니다. 단, 홈 화면은 중복 경로를 만들지 않고 루트 경로(`/`)만 사용합니다.
- **화면 전용 요소:** 특정 화면에서만 사용하는 `data.ts`, `types.ts`, `api.ts`, `hooks.ts` 등은 해당 화면 폴더 내부에 둘 수 있습니다. 여러 화면에서 재사용되면 공통 폴더로 이동합니다.
- **컴포넌트:** 아토믹 디자인 패턴에 따라 `atoms`, `molecules`, `organisms`, `templates` 폴더 내에 각 컴포넌트별 전용 폴더를 생성하여 캡슐화합니다. 단, 스타일 파일은 컴포넌트 폴더가 아니라 `src/assets/styles/components`에서 관리합니다.

## 5. 스타일링 컨벤션 (Styling Convention)

- **SCSS & BEM:** 모든 스타일은 SCSS로 작성하며, BEM(Block Element Modifier) 네이밍 방식을 엄격히 준수합니다. **컴포넌트 내 인라인 스타일(`style={{...}}`)은 절대 금지합니다.**
- **디자인 시스템 활용:** 모든 수치(`margin`, `padding`, `gap`)는 `$spacing-*` 변수를 사용하며, 모든 타이포그래피는 `@include text-style($typo-*)` 믹스인 사용을 원칙으로 합니다.
- **소스맵:** 개발 시 디버깅을 위해 소스맵 생성을 옵션으로 포함합니다.
- **디자인 토큰:** 색상, 배경, 테두리, 그림자, 타이포그래피, 간격, 레이아웃 오프셋 관련 CSS 변수는 `src/assets/styles/base/_tokens.scss`에서만 관리합니다.
- **중복 제거:** 공통 변수 및 믹스인은 `src/assets/styles/base/_variables.scss`, `src/assets/styles/base/_mixins.scss`에 정의하여 재사용합니다.
- **Sass 현대화 (Dart Sass 표준):**
  - `@import` 대신 **`@use`** 및 **`@forward`** 사용을 필수화합니다.
  - `/` 나눗셈 연산 대신 **`math.div()`**를 사용하고, 컬러 함수 등 내장 함수는 **`sass:math`, `sass:color`** 모듈을 명시적으로 호출하여 사용합니다.
  - 레거시 함수(`lighten`, `darken`) 대신 최신 API(`color.adjust`, `color.scale`)를 권장합니다.

## 6. 개발 원칙 및 방어적 코딩 (Development Principles)

- **방어적 프로그래밍:** Optional Chaining(`?.`), Nullish Coalescing(`??`), API 호출 시 `try-catch` 등을 필수 적용합니다.
- **타입 엄격성:** TypeScript 사용 시 **`any` 타입 사용을 원칙적으로 금지**합니다. 모호한 데이터는 `unknown`을 사용하거나 인터페이스를 정의하며, `any`는 기술적으로 회피 불가능한 경우에만 최소한으로 사용합니다.
- **전역 상태 관리:** 전역 상태는 Zustand를 사용하며, `src/stores/authStore.ts`, `src/stores/themeStore.ts`, `src/stores/layoutStore.ts`, `src/stores/appStore.ts`처럼 역할별로 분리합니다.
- **뒤로가기 설정:** 헤더 뒤로가기 버튼은 단순 boolean이 아니라 `history`, `route`, `custom` 타입을 가진 설정 객체로 관리합니다. 커스텀 동작은 설정 파일에 함수를 직접 넣지 않고 `actionKey`로 연결합니다.
- **API 에러 처리:** PocketBase 원본 에러는 화면에서 직접 다루지 않고 `UNAUTHORIZED`, `FORBIDDEN`, `NOT_FOUND`, `VALIDATION`, `NETWORK`, `SERVER`, `UNKNOWN` 코드 기반의 공통 `AppError` 형태로 변환하여 처리합니다.
- **검색/필터/정렬:** 검색어, 태그, 정렬, 필터는 URL query와 동기화하고, 변경 시 `DataList`의 페이지/커서 상태를 초기화합니다.
- **작성/수정/삭제 UX:** 작성 중 내용이 있으면 이탈 전 `Confirm`을 사용하고, 삭제 전에도 반드시 `Confirm`을 사용합니다. 삭제는 기본적으로 soft delete이며 hard delete는 관리자만 수행합니다.
- **삭제 권한:** 게시물과 댓글의 일반 삭제는 작성자 본인 및 관리자만 가능하며, 영구 삭제는 관리자만 수행합니다.
- **파일 업로드:** 프로필/Pics/picLog/에디터 본문 이미지는 `jpg`, `jpeg`, `png`, `webp`만 허용하고, 자유게시판 이미지 삽입은 리치 텍스트 에디터의 이미지 첨부 기능으로 본문 중간에 배치합니다. 이미지 외 일반 파일은 게시글 하단 첨부파일 목록으로 분리합니다. 프로필 이미지는 2MB 이하이며 업로드 시 정사각형 크롭 UI를 거쳐 저장합니다. Pics/picLog/에디터 이미지는 장당 5MB 이하, picLog 5초 순간 영상 후보는 `webm`, `mp4`, `mov`를 입력 후보로 두되 클라이언트 녹화 결과와 서버 저장 포맷 차이를 분리해서 검증합니다. Clips 동영상은 1차로 `mp4`, `webm`, `mov`, 300MB 이하 입력을 허용하고 저장본은 사용자가 선택한 30초 이하 구간을 최대 720p(긴 변 1280px 이하, 짧은 변 720px 이하) MP4로 변환합니다. 동영상 제한값은 화면에 하드코딩하지 않고 재사용 가능한 업로드 프로필과 공통 `VideoUploadField` 옵션으로 관리하며, 서버에서도 ffmpeg 기반 최종 처리와 검증을 수행해야 합니다. 게시판 일반 첨부파일은 `pdf`, `txt`, `md`, `csv`, `doc`, `docx`, `xls`, `xlsx`, `ppt`, `pptx`, `hwp`, `hwpx`, `zip`, `7z`, `rar`, `tar`, `gz`를 허용하며 파일당 10MB 이하, 게시글당 최대 3개, 총합 30MB 이하를 기준으로 합니다. 업로드 이미지는 공통 `Img` 컴포넌트로 렌더링합니다.
- **캐시/실시간:** 생성/수정/삭제 후 관련 목록과 상세 데이터를 무효화하거나 재조회합니다. 좋아요/스크랩은 낙관적 업데이트를 허용하되 실패 시 rollback하며, 모든 PocketBase 구독은 언마운트 시 해제합니다.
- **메모리 최적화:** 컴포넌트 언마운트 시 이벤트 리스너 및 타이머를 반드시 해제(Clean-up)합니다.
- **웹 접근성(A11y):** 시맨틱 HTML5 태그를 최우선으로 사용하며, 적절한 `aria-` 속성과 `alt` 텍스트를 제공합니다.

## 7. 기타 운영 수칙 (Operations)

- **Linting:** 린트 및 포맷팅 적용 시 **수정한 파일**에 대해서만 적용하여 불필요한 전체 프로젝트 변경을 방지합니다.
- **타이틀 설정:** 페이지별로 고유한 타이틀(`document.title`) 및 개별 메타 정보를 설정해야 합니다.

## 8. 데이터베이스 및 백엔드 운영 수칙 (PocketBase)

- **연동 원칙:** 모든 데이터 통신은 PocketBase SDK를 사용하는 것을 원칙으로 합니다.
- **도메인 분리:** 프론트 운영 도메인은 `https://anoju.synology.me`, PocketBase API/Admin 도메인은 `https://pocketbase.anoju.synology.me`로 분리합니다.
- **운영 컨테이너 기준:** 현재 운영 PocketBase 컨테이너 이름은 `anoju-pocketbase-ffmpeg-1`입니다. Docker 확인, 환경 변수 확인, 컨테이너 재시작 안내는 예전 `pocketbase` 컨테이너명이 아니라 이 컨테이너명을 기준으로 합니다. 예: `sudo docker exec anoju-pocketbase-ffmpeg-1 printenv | grep TURNSTILE`.
- **환경 변수:** 프론트 운영 URL은 `.env` 파일의 `VITE_APP_URL`, PocketBase 서버 URL은 `VITE_PB_URL` 변수에 관리하며, 코드 내 하드코딩을 금지합니다.
- **외부 서비스 등록:** OAuth provider, Cloudflare Turnstile, CORS/Redirect 설정 시 프론트 운영 도메인과 PocketBase OAuth redirect 도메인을 구분해서 등록합니다.
- **인증 관리:** 사용자 인증 상태는 SDK에서 제공하는 `authStore`를 활용하며, Zustand 기반 `src/stores/authStore.ts`와 동기화하여 관리합니다.
- **자동 로그인:** 로그인 화면에는 `자동 로그인` 체크 옵션을 제공하며, 사용자가 명시적으로 선택한 경우에만 브라우저 재시작 후에도 세션을 복원합니다. 선택하지 않은 경우 장기 지속 저장을 사용하지 않고 현재 세션 범위에서만 로그인 상태를 유지합니다.
- **자동 로그인 검증:** 앱 시작 시 저장된 토큰은 서버 검증 후에만 유효한 로그인으로 복원하며, 만료/검증 실패/정지/탈퇴 상태가 확인되면 토큰과 사용자 상태를 즉시 정리합니다.
- **자동 로그인 정리:** 로그아웃 또는 자동 로그인 해제 시 저장된 인증 토큰, 사용자 상태, 민감 캐시를 모두 제거합니다. 이메일 미인증 사용자는 자동 로그인으로 복원되더라도 쓰기 권한 제한을 그대로 적용합니다.
- **소셜 로그인/계정 연결:** 이메일/비밀번호 계정을 대표 계정으로 보고 Google, Naver, Kakao는 동일 사용자 계정에 연결되는 로그인 수단으로 관리합니다. 일반 가입 후 마이페이지에서 소셜 계정을 연결할 수 있어야 하며, 소셜 가입 후에도 추후 비밀번호 설정으로 이메일 로그인을 사용할 수 있어야 합니다.
- **소셜 provider 우선순위:** 1차 구현은 Google을 우선하고, Naver/Kakao는 PocketBase provider 설정 및 이메일 제공 동의 범위를 확인한 뒤 순차 적용합니다. 로그인/회원가입 화면에는 실제 활성화된 provider만 노출합니다.
- **소셜 가상 이메일:** Naver/Kakao 등에서 이메일을 제공하지 않으면 provider 사용자 id 기반 `naver_{providerUserId}@oauth.anoju.synology.me`, `kakao_{providerUserId}@oauth.anoju.synology.me` 형식의 가상 이메일을 서버 hook에서 부여합니다. 가상 이메일은 로그인 식별용이며 실제 인증 완료 이메일로 취급하지 않습니다.
- **계정 병합 제한:** 동일 이메일의 인증 완료 계정은 같은 사용자로 연결하는 흐름을 우선 검토하되, 미인증 계정은 이메일 인증 후 연결합니다. 서로 다른 이메일 계정끼리는 자동 병합하지 않습니다. 마지막 로그인 수단은 해제할 수 없습니다.
- **계정 존재 여부 보호:** 로그인 밸리데이션 단계에서 공개 API로 이메일 존재 여부를 사전 확인하지 않습니다. 로그인 실패, 비밀번호 재설정, 소셜 로그인 실패 메시지는 계정 존재 여부를 직접 노출하지 않는 일반화된 한글 메시지를 사용합니다.
- **회원가입 봇 방지:** 일반 이메일 회원가입에는 Cloudflare Turnstile을 적용합니다. 클라이언트는 Sitekey로 token을 발급받고, 서버/PocketBase 훅은 Secret key로 Siteverify 검증을 완료한 뒤에만 회원 생성을 허용해야 합니다. Secret key는 클라이언트 코드와 `VITE_` 환경 변수에 절대 노출하지 않습니다.
- **PocketBase hook 작성 원칙:** `pb_hooks/*.pb.js`는 PocketBase JSVM에서 여러 파일이 같은 런타임 컨텍스트로 로드되거나 callback 실행 시 전역 스코프 참조가 끊기는 문제가 발생할 수 있으므로, hook callback 밖에 공통 helper 함수와 mutable 상태를 두지 않습니다. 각 hook callback 안에서 필요한 helper를 정의하고, 중복이 있더라도 callback 내부에서 자기완결적으로 처리합니다.
- **PocketBase hook 전역 helper 금지:** `verifyTurnstile`, `syncReactionCounts`, `resolveClipVideoPolicy`, `isBlank`처럼 callback이 호출하는 함수는 파일 최상단 전역으로 선언하지 않습니다. 이전에 `clip_video_policy.pb.js`, `reaction_counts.pb.js`, `turnstile.pb.js`에서 전역 helper 참조로 `ReferenceError`가 반복 발생했으므로, 새 hook과 수정 hook 모두 전역 helper 공유 패턴을 금지합니다.
- **PocketBase hook 전역 충돌 방지:** 여러 hook 파일에서 `DEFAULT_*`, `CONFIG_*`처럼 흔한 이름을 전역으로 반복 선언하지 않습니다. 꼭 파일 상단 상수가 필요하면 불변 primitive 값으로 제한하고, 파일 목적이 드러나는 고유 접두사를 붙입니다. 함수, 객체 상태, 캐시, 요청별 데이터는 반드시 callback 내부에 둡니다.
- **PocketBase hook 리뷰 체크:** `pb_hooks` 수정 시 `onRecord`, `onModel`, `router`, `cron` callback 본문에서 참조하는 모든 식별자가 callback 내부에서 선언되었거나 PocketBase가 제공하는 전역 API인지 확인합니다. `rg "function |const |let |var "`로 파일 상단 helper가 남아 있는지 점검하고, 전역 helper가 있으면 배포 전에 callback 내부로 이동합니다.
- **PocketBase hook 실패 정책:** 권한/검증/업로드 제한처럼 요청을 막아야 하는 작업은 `e.next()` 전에 검증하고 명확한 `BadRequestError` 또는 `e.badRequestError()`로 중단합니다. 카운트 동기화, 파생 필드 갱신, 로그 기록처럼 보조 작업은 `try-catch`로 감싸고 실패 시 `$app.logger()` 또는 `e.app.logger()`에 남겨 원 요청이 400으로 롤백되지 않게 설계합니다.
- **댓글/멘션 알림 hook:** 자유게시판, DevLog, Pics의 댓글/답글/`@nickname` 멘션 알림은 `comment_notifications.pb.js`에서 서버 권한으로 생성합니다. 일반 회원은 `users` list rule로 다른 회원 닉네임 검색이 막히므로, 멘션 대상 조회를 프론트에서 수행하지 않습니다. 프론트는 댓글 생성만 담당하고, 댓글 알림 중복 생성을 피하기 위해 클라이언트에서 `post_comment`, `comment_reply`, `mention` 알림을 직접 만들지 않습니다.
- **PocketBase hook 배포 검증:** NAS의 `/volume1/docker/pocketbase/pb_hooks`에 hook 파일을 올린 뒤 운영 컨테이너 `anoju-pocketbase-ffmpeg-1`를 반드시 재시작하고, Admin UI Logs에서 `ReferenceError`, `TypeError`, hook 로딩 오류가 없는지 확인합니다. hook 변경 후에는 관련 API의 create/update/delete 실제 요청을 최소 1회씩 테스트합니다.
- **권한 관리:** 기본 역할은 `guest`, `user`, `admin`이며, 프론트 노출 제어와 PocketBase API Rules를 함께 사용합니다. 이메일 미인증, 정지, 탈퇴 상태는 역할과 별개로 쓰기 액션을 제한할 수 있습니다. 관리자 전용 화면과 관리자 API는 `adminOnly` 라우트와 PocketBase 서버 권한 검증을 함께 적용합니다.
- **테스트 계정 관리:** 게시물/댓글/갤러리 작성 검증용 테스트 계정은 `test1@test.com`, `test2@test.com`, `test3@test.com`을 사용할 수 있습니다. 테스트 계정 비밀번호는 문서, 코드, 저장소에 기록하지 않습니다.
- **에러 핸들링:** API 호출 시 반드시 `try-catch` 및 방어적 코딩 규칙을 적용하여 네트워크 장애에 대비합니다.
- **사용자 메시지:** PocketBase 원본 에러 메시지를 사용자에게 그대로 노출하지 않고, 한글 사용자 메시지로 변환하여 `Toast`, `ErrorState`, 폼 필드 에러에 맞게 표시합니다.
- **실시간 데이터:** 실시간 업데이트가 필요한 기능(채팅, 알림 등)은 PocketBase의 `Subscribe` 기능을 우선적으로 활용합니다.

---

**주의:** 모든 코드는 위 규칙에 어긋날 경우 승인되지 않습니다. 특히 한글 주석과 폴더 체계화 원칙을 철저히 지켜주십시오.
