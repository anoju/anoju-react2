import FreeBoardWrite from '../../FreeBoard/Write/Index';
import { DEV_LOG_CONFIG } from '../../boardConfig';

const DevLogWrite = () => <FreeBoardWrite config={DEV_LOG_CONFIG} />;

export default DevLogWrite;
